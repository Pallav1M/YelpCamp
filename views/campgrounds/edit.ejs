<% layout('layout/boilerplate.ejs') %>

    <!-- *************************************************************************************************************** -->
    <!-- The following code is the initial code. Not susre why the ejs tags are not commented  -->
    <!-- *************************************************************************************************************** -->

    <!-- <h1> Edit Campground</h1>
<form action="/campgrounds/<%= campgrounds._id %>?_method=PUT" method="POST">
    <div>
        <lable for="title">Title</lable>
        <-- name is used to group contents together -->
    <!-- <input type="text" id="title" name="campgrounds[title]" value="<%= campgrounds.title %>"> -->
    <!-- </div> -->
    <!-- <div> -->
    <!-- <lable for="location">Location</lable> -->
    <!-- name is used to group contents together -->
    <!-- <input type="text" id="location" name="campgrounds[location]" value="<%= campgrounds.location %> "> -->
    <!-- </div> -->
    <!-- <button> Update Campground</button> -->
    <!-- </form> -->
    <!-- <a href="/campgrounds/<%= campgrounds._id %>">Back to Campgrounds</a> -->

    <div class="row">
        <h1 class="text-center">Edit Campground</h1>
        <!-- if i want the middle 6 in the center, i can use offset-3 -->
        <div class="col-md-6 offset-md-3">
            <form action="/campgrounds/<%= campgrounds._id %>?_method=PUT" method="POST" novalidate
                class="validated-form" enctype="multipart/form-data">
                <div class="mb-3">
                    <!-- From https://getbootstrap.com/docs/5.0/forms/input-group/ -->
                    <label class="form-label" for="title">Title</label>
                    <!-- name is used to group contents together -->
                    <input class="form-control" type="text" id="title" name="campgrounds[title]"
                        value="<%= campgrounds.title %>" required>
                    <div class="valid-feedback">
                        Looks Good
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="location">Location</label>
                    <!-- name is used to group contents together -->
                    <input class="form-control" type="text" id="location" name="campgrounds[location]"
                        value="<%= campgrounds.location %>" required>
                    <div class="valid-feedback">
                        Looks Good
                    </div>
                </div>
                <!-- <div class="mb-3">
                    <label class="form-label" for="image">Add Images</label>
                    <input type="file" name="image" id="image" multiple>
                </div> -->
                <div class="mb-3">
                    <label class="form-label" for="price">Campground Price</label>
                    <div class="input-group">
                        <span class="input-group-text" id="price-label">$</span>
                        <input type="text" class="form-control" id="price" placeholder="0.00" aria-label="Price"
                            aria-describedby="price-label" name="campgrounds[price]" value="<%= campgrounds.price %>"
                            required>
                        <div class="valid-feedback">
                            Looks Good
                        </div>
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="description">Description</label>
                    <!-- name is used to group contents together -->
                    <input class="form-control" type="textarea" id="description" name="campgrounds[description]"
                        value="<%= campgrounds.description %>" required></input>
                    <div class="valid-feedback">
                        Looks Good
                    </div>
                </div>
                <!-- <div class="mb-3">
                    <div class="form-file custom-file">
                        <input type="file" class="form-file-input" name="image" id="image" multiple>
                        <label class="form-file-label" for="image">
                            <span class="form-file-text custom-file-label">Add more images</span>
                            <span class="form-file-button"> Browse</span>
                        </label>
                    </div>
                </div> -->
                <div class="mb-3">
                    <input type="file" name="image" id="image" multiple>
                </div>
                <div class="mb-3">
                    <% campgrounds.images.forEach(function(img,i) { %>
                        <img src="<%= img.thumbnail %> " alt="" class="img-thumbnail">
                        <div class="form-check-inline">
                            <input type="checkbox" id="img-<%= i %> " name="deleteImages[]" value="<%= img.filename %>">
                        </div>
                        <label for="img-<%= i %>">Delete?</label>
                        <% })%>
                </div>
                <div class=" mb-3">
                    <button class="btn btn-info"> Update Campground</button>
                </div>
                <a href="/campgrounds/<%= campgrounds._id %>">Back to Campgrounds</a>
            </form>
        </div>
    </div>